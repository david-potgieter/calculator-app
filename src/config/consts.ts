export enum MODE {
  MODE_ON = 'mode-on',
  MODE_OFF = 'mode-off',
}

export enum OPERATIONS {
  ADD_OPERATION = 'addition',
  DIVIDE_OPERATION = 'divide',
  EQUAL_OPERATION = 'equal',
  EXPONENT_OPERATION = 'exponent',
  MULTIPLY_OPERATION = 'multiply',
  SUBTRACT_OPERATION = 'subtract',
}

export enum ACTIONS {
  CLEAR_ACTION = 'clear',
  DELETE_ACTION = 'delete',
}
